apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: weight-dashboard
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/franchyze923/frans-homelab.git
    targetRevision: main
    path: gitops/workloads/weight-dashboard
  destination:
    server: https://kubernetes.default.svc
    namespace: weight-dashboard
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
