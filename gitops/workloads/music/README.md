# music

Navidrome at `navidrome.franpolignano.com` plus a nightly library sync job.
Deployed 2026-07-11. Standard storage patterns (RBD state + nightly NFS
backup with restore initContainer; library on NFS, not backed up).
